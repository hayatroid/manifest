# ref: https://dozzle.dev/guide/getting-started#standalone-docker

services:
  dozzle:
    image: amir20/dozzle:latest
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    ports:
      - 3001:8080
    restart: unless-stopped
